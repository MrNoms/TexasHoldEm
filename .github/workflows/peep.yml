name: Machine Properties
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - run: git branch -a
      - run: git diff --name-only ${{ github.ref_name }}..remotes/origin/rayshaun
