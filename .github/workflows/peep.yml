name: Machine Properties
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls -a
      - run: ls -a TexasHoldEm
      - run: ls -a TexasHoldEmTest
      
