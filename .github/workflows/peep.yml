name: Machine Properties
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: git branch -a
      - run: git checkout -b rayshaun
      - run: git diff ${{ github.ref_name }}..rayshaun
